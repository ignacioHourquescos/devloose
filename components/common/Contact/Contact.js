import React from "react";
import { Styled } from "./styles.js";

const Contact = (props) => {
	return (
		<Styled.Inner>
			<Styled.Container>s</Styled.Container>
		</Styled.Inner>
	);
};

export default Contact;
